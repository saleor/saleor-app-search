fragment ProductData on Product {
  id
  name
  description
  slug
  thumbnail {
    url
  }
  attributes {
    ...ProductAttributesData
  }
  channelListings {
    id
    visibleInListings
    channel {
      slug
      currencyCode
    }
  }
}
