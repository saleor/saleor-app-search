query ProductsDataForImport($first: Int, $after: String) {
  products(first: $first, after: $after) {
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        ...ProductWebhookPayload
      }
    }
  }
}
